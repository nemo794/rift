# Runtime deps for the nisar2cog OGC entrypoint. rift itself pulls numpy/scipy/h5py/
# rasterio/pyproj/jsonargparse/tqdm from pyproject.toml when installed with `pip install .`;
# this file is a convenience for the local Hub-terminal pre-flight test.
requests
numpy>=1.25
scipy
h5py>=3.0
rasterio>=1.4
pyproj
jsonargparse>=4.27
tqdm
